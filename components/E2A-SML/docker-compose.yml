services:
  sml-extractor:
    build: .
    ports:
      - "7861:7861"
    environment:
      E2A_PATH: /app/data
    volumes:
      # Persist the standalone voice library and BookNLP models.
      - ./data:/app/data
      # Mount a local output directory to persist generated SML files
      - ./output:/app/output
